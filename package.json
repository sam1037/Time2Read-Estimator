{
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "scripts": {
    "build": "webpack",
    "zip": "npm run build && zip -r dist.zip dist"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "camelcase": "^8.0.0"
  }
}
